=== Logbook

[width="100%", cols="2,2,2,5,1", options="header", frame=all, grid=all]
|===
| Section Name | Member | Added or Modified | Description | Milestone

| 4.1 TDD
| Gerardo Ruiz
| Added
| Filled TDD with 5-iteration walkthrough of `generate_available_slots`: red/green/refactor cycle, 12 passing tests, traceability table.
| M2

| 4.2 BDD and BDD Tool
| Gerardo Ruiz
| Added
| Filled BDD with Gherkin feature (Background + 3 scenarios), pytest-bdd step definitions, execution output (3 passed), traceability table.
| M2

| 4.3 Debugger
| Gerardo Ruiz
| Added
| Filled debugging with two hypothesis-driven sessions: pdb breakpoint session and Chrome DevTools Network panel session (303ms gap finding).
| M2

| 2.1.1 Rough Sketch
| Gerardo Ruiz
| Fixed
| Removed all inline Observation and Derived concepts annotations. Raw quotes only. Fixed contradictory NOTE.
| M2

| LTT 4.4 BDD
| Gerardo Ruiz
| Improved
| Added Background block, fixed status to Requested, added traceability table.
| M2

| documentation.adoc
| Gerardo Ruiz
| Fixed
| Uncommented LTT section include.
| M2

| 4 Specific Class Topics
| Julian Vivas
| Added & Modified
| Added a new section to document specific class topics covered in the course, including TDD, BDD, and debugging techniques.
| M2

| 3.3.6 Mutation Testing
| Natanael Batista
| Added
| Added a new section to document the mutation testing process, including examples of surviving and killed mutations, and their implications for test effectiveness.
| M2

| 1.2.1 Current Situation, Needs, Ideas
| Alejandro Lopez
| Added & Modified
| Followed professor's feedback to revise where needed. Added a new references section.
| M2

| 1.2.2 Needs
| Alejandro Lopez
| Removed & Added
| Revised and rewrote the Needs section as per the professor's feedback.
| M2

| 1.3.1 Scope & Span
| Alejandro Lopez
| Removed & Added
| Revised and rewrote scope and span as per professor's feedback.
| M2

| 1.3.2 Synopsis
| Alejandro Lopez
| Removed & Added
| Revised and rewrote synopsis as per professor's feedback.
| M2

| 1.4 Other Activities
| Alejandro Lopez
| Removed & Added
| Revised and rewrote "Other Activities than Just Developing Source Code" per professor's feedback.
| M2

| 3.3.5 Formal Verification
| Julian Vivas
| Added
| Added a Hoare logic and weakest-precondition verification for the project booking operation, documenting preconditions, postconditions, invariants, and the resulting correctness argument.
| M2

| 2.3.1 Implementation
| Natanael Batista
| Removed & Added
| Revised and rewrote "Selected Fragments of the Implementation" per professor's feedback.
| M2

| 2.1.3 Narrative
| Juan Ortiz Abouomar
| Changed
| Revised and rewrote paragraphs 1, 3, and 4 of "Narrative" per professor's feedback.
| M2

| 3.3.3 BDD Tooling and Execution
| Michael Martinez Curbelo
| Added & Modified
| Documented BDD tooling rationale, automated `pytest-bdd` execution flow, CI pipeline integration, and verified run evidence (`3 passed`) for Milestone 2 compliance.
| M2

| 4.4 Usability Testing
| Jean Sanchez Felix
| Added
| Expanded the section into a complete, measurable usability study: defined scenario and objectives, participant rationale, setup and monitoring method, minimum success criteria, results analysis, usability pain points, and design recommendations derived from findings.
| M2

| 4.5 Property-based Testing
| Jean Sanchez Felix
| Added & Modified
| Rewrote the section to document `pytest` + `hypothesis` usage, defined scheduling invariants, included non-trivial property-based test examples, and explained how domain types (`TimeRange`, `DailyAvailability`) and typed function contracts improve correctness and readability.
| M2

| 4.6 Fuzzing
| Jean Sanchez Felix
| Added & Modified
| Added web-focused fuzzing strategy, malformed/boundary/random-flow/injection scenario examples, tooling approach, and explanation of how fuzzing complements property-based and behavioral testing to improve robustness and security.
| M2

| 2.3.1 Implementation
| Julian Vivas
| Added & Modified
| Added Lecture Topic Task documentation for domain typing in appointment scheduling, including `PatientID`, `Doctor`, `TimeSlot`, `Appointment`, a non-trivial typed booking function, and Unit/None return justification.
| M3

| Logbook
| Julian Vivas
| Added & Modified
| Added a Milestone column to identify whether each logged change belongs to M2 or M3, and recorded the M3 domain-typing implementation entry separately from carried-over M2 entries.
| M3

| 2.1.4 Events, Actions, and Behaviors
| Julian Vivas
| Modified
| Split all 5 conflated entries into separate action and event entries (Appointment Requested, Appointment Canceled, Provider Availability Modified, Queue Appointment Reminder, Join Waitlist). Fixed Referral Recorded type by removing spurious entity-creation qualifier. Updated all Conceptual Function Signature cross-references to name the split action and event separately.
| M3

| 2.1.2 Terminology
| Julian Vivas
| Modified
| Reclassified conflated domain terms: Cancel Appointment as an action + added Appointment Canceled as an event; Referral as an entity + added Referral Recorded as an event; Insurance Authorization as an entity; split coverage authorization into Request Coverage Authorization as an action + Coverage Authorized as an event. Fixed Appointment definition to remove informal "time block" reference. Added formal NOTE defining actor as an entity role rather than a separate classification.
| M3

| README, backend scheduling service, CI workflow
| Jean Sanchez
| Added & Modified
| Added explicit branch/repository guidance to README, created `backend/app/services/scheduling.py` to match the documented unit-tested scheduling logic, and updated CI/load-smoke infrastructure to reflect artifacts that exist on `main`.
| M3

| 1.2.1, 1.2.2, 1.2.3, 1.4
| Jean Sanchez
| Removed & Added
| Rewrote unsupported claims and generic ideas into evidence-aware current situation, stakeholder needs, need-linked ideas, and project-specific non-coding activities.
| M3

| 2.1.2, 2.1.3, 2.1.4
| Jean Sanchez
| Removed & Added
| Rebuilt terminology, narrative, and core operations to separate entities, actions, events, and behaviors clearly; clarified slot vs appointment, provider vs doctor, and state-transforming function signatures.
| M3

| 2.2.1, 2.2.3, 2.2.4, 2.2.5
| Jean Sanchez
| Removed & Added
| Reorganized requirements into a layered structure, added missing waitlist/confirmation/cancellation-policy requirements, narrowed interface requirements, and replaced pseudo-machine requirements with branch-accurate machine requirements.
| M3

| 2.3.1, 3.1, 3.2, 3.3.1-3.3.6
| Jean Sanchez
| Removed & Added
| Aligned implementation, analysis, validation, TDD, BDD, load, Hoare-logic, and mutation-testing sections with current repository artifacts; removed nonexistent IDs/modules and corrected overclaims about implementation and testing scope.
| M3

| 2.1.4, 3.1, 3.2
| Jean Sanchez
| Modified
| Performed Milestone 3 consistency sweep: replaced obsolete references (`DR-*`, `ClinicSystem`, `BehaviorTrace`), aligned event/state names with terminology (`Requested`, `PendingApproval`, `Confirmed`, `Cancelled`, `NoShow`), updated function signatures to terminology-safe abstractions, and synchronized validation references to existing requirement IDs across domain/interface/machine sections.
| M3

| 3.3.1 TDD
| Jean Sanchez
| Modified
| Rewrote TDD as an applied report based on existing code (`backend/app/services/scheduling.py`) and tests (`backend/tests/unit/test_scheduling.py`): documented concrete RED/GREEN/REFACTOR iterations, listed specific test refinements, addressed `DailyAvailability(slot_minutes=0)` design tradeoff, and aligned claims with current BDD scope on branch.
| M3

| 3.3.2 BDD Scenario Design
| Jean Sanchez
| Modified
| Rewrote scenario section to match executable BDD artifacts on branch (`appointment_booking.feature` + steps), replaced overbroad claims with explicit implemented-vs-planned split, mapped concrete test cases to valid requirement IDs, and added missing design corrections for insurance-pending confirmation and 1:55 PM cutoff boundary behavior.
| M3

| 3.3.3 BDD Tooling and Execution, BDD feature/steps
| Jean Sanchez
| Modified
| Broadened executable BDD suite from 3 to 7 scenarios by adding insurance-pending flow, insurance-clear confirmation, reschedule-cutoff boundary case (1:55 PM vs 2:00 PM), and waitlist reassignment. Updated tooling/execution documentation to reflect CI branch scope (`main` + `Documentation`) and expanded scenario-to-requirement traceability table.
| M3

| 3.3.4 Load Testing, backend CI workflow, load scripts
| Jean Sanchez
| Modified
| Replaced overclaimed load-testing narrative with branch-accurate documentation, added deterministic CI load-smoke script (`backend/tests/load/locust_smoke.py`), and integrated Locust load-smoke execution into `.github/workflows/backend-tests.yml` by starting backend in test mode and running concurrent `/api/health` probes.
| M3

| 3.3.5 Hoare Logic / Formal Verification
| Jean Sanchez
| Modified
| Clarified formal relationship between `booked` and `slotAvailable` by defining `booked` as a derived predicate from appointment-slot association and `Confirmed` status, then stated slot-availability consistency equivalence for the same doctor-slot pair to remove ambiguity in pre/postcondition reasoning.
| M3

| 3.3.6 Mutation Testing
| Jean Sanchez
| Modified
| Rewrote mutation-testing methodology to mutate backend production code (not tests), separated backend logic scope from frontend UI scope, replaced figure/screenshot reporting with text tables, added broader multi-module mutation target plan with readiness levels, corrected score interpretation (<50% = significant gaps), and bounded conclusions to mutated module/test-suite scope.
| M3

| Section structure update (4.x moved into 3.3.7-3.3.9)
| Jean Sanchez
| Modified
| Finalized milestone structure by consolidating usability, property-based testing, and fuzzing under Section 3 and updating internal cross-references (e.g., property-based testing reference in fuzzing now points to Section 3.3.8).
| M3

| Final completeness alignment (sections 2 & 3, CI)
| Jean Sanchez
| Modified
| Re-enabled Section 3 topic integration for domain/function completeness, extended CI backend workflow to execute unit tests in addition to BDD and load-smoke checks, and added explicit acceptance-evidence wording in Section 3.3.3 tying implemented MVP features to automated BDD scenarios.
| M3
|===
