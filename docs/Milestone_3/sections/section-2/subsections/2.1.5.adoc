[.removed]
==== *Function Signatures*

Notation:

* `A >< B -> C` consumes A and B and produces C.

* Updated state is returned.

* `Result X Error` models success or failure.

* `Option X` models a possible absence of result.

Core Operations
[source,text]
----
[.changed]#publishSlot : generate_available_slots(availability: DailyAvailability) -> List[datetime]#

[.changed]#requestAppointment : book_admin_appointment(*, patient_id: str, doctor_id: str, appointment_date: date, appointment_time: time, status: Literal["pending", "confirmed"] = "confirmed", notes: str | None = None, accept_waitlist: bool = False, allow_override: bool = False) -> tuple[dict | None, tuple[dict, int] | None]#

[.changed]#confirmAppointment : update_appointment(appointment_id: str) -> Response#

[.changed]#cancelAppointment : cancel_appointment(appointment_id: str) -> Response#

[.changed]#rescheduleAppointment : reschedule_appointment(appointment_id: str, new_datetime: str) -> Response#

[.changed]#modifyAvailability : update_appointment(appointment_id: str) -> Response#
----

Waitlist
[source,text]
----
[.changed]#joinWaitlist : _count_waitlist_for_day(doctor_id: str, day: date) -> int#

[.changed]#selectWaitlistCandidate : book_admin_appointment(*, patient_id: str, doctor_id: str, appointment_date: date, appointment_time: time, status: Literal["pending", "confirmed"] = "confirmed", notes: str | None = None, accept_waitlist: bool = False, allow_override: bool = False) -> tuple[dict | None, tuple[dict, int] | None]#

[.changed]#reassignSlot : book_admin_appointment(..., accept_waitlist=True, allow_override=True) -> tuple[dict | None, tuple[dict, int] | None]#
----

Notifications
[source,text]
----
[.changed]#queueReminder : get_profile(user_id: str) -> JSON#

[.changed]#issueReminder : update_profile(user_id: str) -> JSON#
----

Lifecycle Updates
[source,text]
----
[.changed]#markNoShow : update_appointment(appointment_id: str) -> Response#

[.removed]#recordReferral : ReferralRegistry >< Patient >< Provider >< Referral -> ReferralRegistry >< Result Unit Error#

[.changed]#recordInsuranceApproval : get_profile(user_id: str) -> JSON ; update_profile(user_id: str) -> JSON#
----

Search & Authorization
[source,text]
----
[.removed]#searchProviders : ClinicSystem >< Specialty -> List Provider#

[.changed]#listAvailableSlots : generate_available_slots(availability: DailyAvailability) -> List[datetime]#

[.changed]#authorizeAction : checkAccess(session: SupabaseSession, userRoleCache: SessionStorage) -> Promise<void>#
----

Behavior
[source,text]
----
bookingBehavior : ClinicSystem >< Patient
 -> ClinicSystem >< BehaviorTrace
----