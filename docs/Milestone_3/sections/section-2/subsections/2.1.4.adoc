==== *Events, Actions, and Behaviors*

===== TimeSlotPublished

- *Type:* Event  
- *Definition:* [.changed]#An appointment slot has become available to the community.#
- *Trigger:* Follows the completion of the PublishTimeSlot action.
- *Observable Condition / Resulting State:* A new appointment slot exists with status *available* and is visible to patients for discovery and booking.  
- Reference to sketch: Providers define working hours; Jose cannot see other sites’ calendars to compare newly published slots.  

===== RequestAppointment

- *Type:* [.removed]#Combined classification# [.added]#Action#
- *Actor:* [.added]#Patient#
- *Definition:* [.changed]#A patient selects an available time slot and submits a booking request.#
- *Trigger:* A patient selects a slot and submits a request.
- *Observable Condition / Resulting State:* [.changed]#The system processes the request; a pending appointment record is created.#
- Reference to sketch: Carmen redialed 23 times starting 5 Feb 2026 at 8:02 a.m. to request a slot.

===== [.added]#AppointmentRequestSubmitted#

- *Type:* Event
- *Definition:* The instant the system records a patient's booking request.
- *Trigger:* Follows the completion of the RequestAppointment action.
- *Observable Condition / Resulting State:* A request record exists; the appointment enters a confirmation or waitlist process depending on constraints.
- Reference to sketch: Carmen redialed 23 times starting 5 Feb 2026 at 8:02 a.m. to request a slot.

===== AppointmentConfirmed

- *Type:* Event  
- *Definition:* The instant an appointment request transitions into a confirmed booking.  
- *Trigger:* Follows the completion of the ConfirmAppointment action.  
- *Observable Condition / Resulting State:* The slot status becomes *reserved*; the appointment is part of the clinic schedule and reminders may be scheduled.  
- Reference to sketch: Clinics maintain structured schedules once slots are assigned.  

===== CancelAppointment

- *Type:* [.removed]#Combined classification# [.added]#Action#
- *Actor:* [.added]#Patient or clinic staff#
- *Definition:* [.changed]#A patient or clinic staff member withdraws a previously confirmed or requested appointment.#
- *Trigger:* A patient or clinic cancels the appointment.
- *Observable Condition / Resulting State:* [.changed]#The cancellation request is processed; the associated time slot is marked for release.#
- Reference to sketch: Laura flips through a notebook and spreadsheet to reassign cancellations.

===== [.added]#AppointmentCancelled#

- *Type:* Event
- *Definition:* The instant a previously confirmed or requested appointment has been withdrawn and the associated time slot has been released.
- *Trigger:* Follows the completion of the CancelAppointment action.
- *Observable Condition / Resulting State:* The slot becomes *available* again; downstream processes (e.g., waitlist handling) may be triggered.
- Reference to sketch: Laura flips through a notebook and spreadsheet to reassign cancellations.

===== WaitlistedPatientAssigned

- *Type:* Event  
- *Definition:* The instant a released slot is allocated to a selected patient from a waiting list.  
- *Trigger:* Follows the completion of the AssignWaitlistedPatient action after a cancellation or new availability makes a slot eligible.  
- *Observable Condition / Resulting State:* The slot becomes *reserved* for the selected patient; notification is issued.  
- Reference to sketch: Informal priority lists used when earlier openings appear (same workflow Laura described).  

===== ModifyProviderAvailability

- *Type:* [.removed]#Combined classification# [.added]#Action#
- *Actor:* [.added]#Provider#
- *Definition:* [.changed]#A provider updates their working schedule or availability constraints in the system.#
- *Trigger:* A provider changes working hours, location, or service capacity.
- *Observable Condition / Resulting State:* [.changed]#The provider's schedule is updated; the system checks for conflicting appointments.#
- Reference to sketch: Providers split time across clinics and schedules change frequently; Miguel found doors locked after 6:00 p.m.

===== [.added]#ProviderAvailabilityModified#

- *Type:* Event
- *Definition:* A provider's working schedule or availability constraints have been updated in the system.
- *Trigger:* Follows the completion of the ModifyProviderAvailability action.
- *Observable Condition / Resulting State:* Availability records are updated; conflicting appointments may require rescheduling or notification.
- Reference to sketch: Providers split time across clinics and schedules change frequently; Miguel found doors locked after 6:00 p.m.

===== QueueAppointmentReminder

- *Type:* [.removed]#Combined classification# [.added]#Action#
- *Actor:* [.added]#Scheduling system#
- *Definition:* [.changed]#The scheduling system queues a reminder notification for an upcoming appointment based on configured timing rules.#
- *Trigger:* [.changed]#The appointment time approaches and a reminder becomes due according to notification preferences.#
- *Observable Condition / Resulting State:* [.changed]#A pending notification record exists in the notification queue.#
- Reference to sketch: Staff sometimes send manual reminders from personal phones.

===== [.added]#AppointmentReminderIssued#

- *Type:* Event
- *Definition:* A reminder notification has been dispatched to the patient for an upcoming appointment.
- *Trigger:* Follows the completion of the QueueAppointmentReminder action.
- *Observable Condition / Resulting State:* A notification record exists; the patient has been informed through available communication channels.
- Reference to sketch: Staff sometimes send manual reminders from personal phones.

===== NoShowRecorded

- *Type:* Event  
- *Definition:* A patient fails to attend a scheduled appointment.  
- *Trigger:* The appointment time passes without attendance confirmation.  
- *Observable Condition / Resulting State:* The appointment is marked as *no-show*; attendance metrics are updated.  
- Reference to sketch: Dr. Rivera reported fluctuating no-shows.  

===== ReferralRecorded

- *Type:* [.changed]#Event#
- *Definition:* The instant a referral authorizing a patient to access a consultation is registered.  
- *Trigger:* Follows the completion of the RecordReferral action.  
- *Observable Condition / Resulting State:* A referral entity exists; eligibility constraints for booking are satisfied.  
- Reference to sketch: Referral requirements introduce coordination steps.  

===== JoinWaitlist

- *Type:* [.removed]#Combined classification# [.added]#Action#
- *Actor:* [.added]#Patient#
- *Definition:* [.changed]#A patient opts into a waitlist for a desired specialty or provider when no appointment slots are immediately available.#
- *Trigger:* A patient opts into a waitlist due to lack of availability.
- *Observable Condition / Resulting State:* [.changed]#The patient's waitlist registration request is processed.#
- Reference to sketch: Natalia joined a waitlist in February 2026 and never received status updates.

===== [.added]#WaitlistEntryCreated#

- *Type:* Event
- *Definition:* The instant a patient's interest in future appointment availability is recorded in the waitlist.
- *Trigger:* Follows the completion of the JoinWaitlist action.
- *Observable Condition / Resulting State:* A waitlist entry exists; the patient becomes eligible for reassignment when slots open.
- Reference to sketch: Natalia joined a waitlist in February 2026 and never received status updates.

===== Conceptual Function Signatures

*Purpose:* These conceptual functions describe the core domain operations from a system-independent perspective, including their purpose, required inputs, outputs, and dependencies. They map to the domain events and actions defined above.

====== [.changed]#Insert Appointment#

----
_insert_appointment(*, patient_id: str, doctor_id: str, clinic_id: str, appointment_datetime: datetime, appointment_type: BookingType, status: Literal["pending", "confirmed"] = "confirmed", notes: str | None = None): tuple[dict | None, tuple[dict, int] | None]
----

- *Description:* [.changed]#Inserts a new appointment record into the appointments table and makes it discoverable.#
- *Conceptual Inputs:* [.changed]#Patient ID, doctor ID, clinic ID, appointment datetime, booking type, optional status (defaults to confirmed), and optional notes.#
- *Conceptual Output:* [.changed]#The created appointment record as a dictionary, or an error tuple with details and HTTP status code.#
- *Dependencies:* Requires provider authorization and valid scheduling constraints.
- *Related Domain Events/Actions:* [.changed]#Action that produces *TimeSlotPublished* (event).#

====== [.changed]#Get Available Slots / Get Appointments#

----
getAvailableSlots(doctorId: string, date: string): Promise<{ slots: Array<{ start_time: string, end_time: string, available: boolean }> }>

getAppointments(status?: string): Promise<Appointment[]>

getAppointment(id: string): Promise<Appointment>
----

- *Description:* [.changed]#Retrieves available time slots for a given doctor on a specific date, fetches a list of appointments optionally filtered by status, or fetches a single appointment by ID. In reschedule mode, the UI layer automatically fetches confirmed appointments on mount.#
- *Conceptual Inputs:* [.changed]#For slot lookup: doctor ID and date (YYYY-MM-DD). For appointment list: optional status filter. For single appointment: appointment ID.#
- *Conceptual Output:* [.changed]#A list of time slots with availability flags, a list of appointments, or a single appointment record.#
- *Dependencies:* [.changed]#Slot lookup is public (no auth required); appointment queries require authentication. Reschedule mode fetch is triggered by UI state.#
- *Related Domain Events/Actions:* [.changed]#Triggers the *RequestAppointment* (action), which produces the *AppointmentRequestSubmitted* (event).#

====== [.changed]#Validate Requested Slot#

----
_validate_requested_slot(doctor_id: str, day: date, start_time: time): tuple[datetime | None, dict | None, int | None]
----

- *Description:* [.changed]#Validates that a requested time slot falls within the doctor's generated availability for a given date before confirming the appointment.#
- *Conceptual Inputs:* [.changed]#Doctor ID, appointment date, and requested start time.#
- *Conceptual Output:* [.changed]#The resolved datetime if valid, or an error dictionary with an HTTP status code if the slot is unavailable.#
- *Dependencies:* [.changed]#Requires fetched doctor availability and generated slot list for the given date.#
- *Related Domain Events/Actions:* [.changed]#Action that produces *AppointmentConfirmed* (event).#

====== Cancel Appointment

----
cancelAppointment(id: number, reason?: string, cancelledById?: number | null): Promise<void>
----

- *Description:* [.changed]#Sends a cancellation request for an existing appointment via a PUT call, optionally recording the reason and the admin performing the action.#
- *Conceptual Inputs:* [.changed]#Appointment ID, optional cancellation reason, and optional ID of the admin performing the cancellation.#
- *Conceptual Output:* [.changed]#A resolved promise on success, or a rejected promise on failure.#
- *Dependencies:* [.changed]#Appointment must exist and be cancelable; requires authentication.#
- *Related Domain Events/Actions:* [.changed]#Triggers the *CancelAppointment* (action), which produces the *AppointmentCancelled* (event).#

====== Reschedule Appointment

----
rescheduleAppointment(appointmentId: string, newDatetime: string): Promise<void>
----

- *Description:* [.changed]#Sends a reschedule request for an existing appointment to a new datetime via a POST call to the scheduling endpoint.#
- *Conceptual Inputs:* [.changed]#Appointment UUID and the new target datetime in ISO 8601 format.#
- *Conceptual Output:* [.changed]#A resolved promise on success, or a rejected promise on failure.#
- *Dependencies:* [.changed]#Appointment must exist; new datetime must correspond to a compatible available slot; requires authentication.#
- *Related Domain Events/Actions:* [.changed]#Action that may produce appointment and slot state-change events.#

======  [.changed]#Update Appointment#

----
update_appointment(appointment_id: str): Response
----

- *Description:* [.changed]#Updates one or more fields of an existing appointment via a PUT request, including doctor, datetime, status, and clinic. If the doctor or datetime changes, the new slot is validated and reserved before the record is written.#
- *Conceptual Inputs:* [.changed]#Appointment ID (path parameter); JSON body with any combination of doctor_id, appointment_datetime (ISO-8601), status, and clinic_id.#
- *Conceptual Output:* [.changed]#A JSON response with the updated appointment record on success, or an error message with an appropriate HTTP status code on failure.#
- *Dependencies:* [.changed]#Appointment must exist; scheduling changes require slot reservation via try_reserve_slot and clinic resolution via _get_doctor_clinic_id; requires authentication.#
- *Related Domain Events/Actions:* [.changed]#Triggers the *ModifyProviderAvailability* (action), which produces the *ProviderAvailabilityModified* (event).#

====== Book Admin Appointment / Count Waitlist for Day

----

_count_waitlist_for_day(doctor_id: str, day: date): int

book_admin_appointment(*, patient_id: str, doctor_id: str, appointment_date: date, appointment_time: time, status: Literal["pending", "confirmed"] = "confirmed", notes: str | None = None, accept_waitlist: bool = False, allow_override: bool = False): tuple[dict | None, tuple[dict, int] | None]
----

- *Description:* [.changed]#Orchestrates the full admin booking flow for a given doctor and patient. Attempts to place a MAIN booking at the requested slot; if full and accepted, assigns a WAITLIST booking to the next available slot on the same day subject to a daily cap; if no waitlist placement is possible and override is allowed, books an OVERRIDE at the requested slot; otherwise rejects. The helper counts active waitlist appointments for a doctor on a given day to enforce the daily limit.#
- *Conceptual Inputs:* [.changed]#For the helper: doctor ID and target date. For the orchestrator: patient ID, doctor ID, appointment date and time, optional status (defaults to confirmed), optional notes, a flag to accept waitlist placement, and a flag to allow override booking.#
- *Conceptual Output:* [.changed]#The helper returns an integer count. The orchestrator returns a result dictionary with the appointment record, booking type, assigned and requested datetimes, and a message; or an error tuple with details and HTTP status code.#
- *Dependencies:* [.changed]#Requires slot validation via _validate_requested_slot, clinic resolution via _get_doctor_clinic_id, capacity checks via _get_main_capacity and _count_appointments_by_type, next-slot lookup via _find_next_available_slot_same_day, and record insertion via _insert_appointment.#
- *Related Domain Events/Actions:* [.changed]#Triggers the *JoinWaitlist* (action), which produces the *WaitlistEntryCreated* (event).#

====== [.removed]#Select Waitlist Candidate#

----
selectWaitlistCandidate(waitlist: Waitlist, slot: TimeSlot): Option Patient
----

- *Description:* Selects an eligible patient from the waitlist for a newly available slot.
- *Conceptual Inputs:* Waitlist, time slot.
- *Conceptual Output:* Optional patient.
- *Dependencies:* Depends on prioritization rules.
- *Related Domain Events/Actions:* Supports the *AssignWaitlistedPatient* action and the *WaitlistedPatientAssigned* event.

====== [.removed]#Reassign Slot#

----
reassignSlot(waitlist: Waitlist, appointmentBook: AppointmentBook, timeSlotCatalog: TimeSlotCatalog, notificationQueue: NotificationQueue, slotId: TimeSlotId): Option AppointmentId
----

- *Description:* Assigns an available slot to a waitlisted patient and notifies them.
- *Conceptual Inputs:* Waitlist, appointment book, slot catalog, notification queue, slot ID.
- *Conceptual Output:* Optional appointment ID.
- *Dependencies:* Requires candidate selection and slot availability.
- *Related Domain Events/Actions:* Action that produces *WaitlistedPatientAssigned* (event).

====== [.removed]#Queue Reminder#

----
queueReminder(notificationQueue: NotificationQueue, appointment: Appointment): NotificationQueue
----

- *Description:* Adds a reminder notification for a scheduled appointment.
- *Conceptual Inputs:* Notification queue, appointment.
- *Conceptual Output:* Updated notification queue.
- *Dependencies:* Appointment must be confirmed.
- *Related Domain Events/Actions:* Supports the *QueueAppointmentReminder* (action) and *AppointmentReminderIssued* (event).

====== [.removed]#Issue Reminder#

----
issueReminder(notificationQueue: NotificationQueue, channel: Channel): Result Unit Error
----

- *Description:* Sends queued reminders through a communication channel.
- *Conceptual Inputs:* Notification queue, channel.
- *Conceptual Output:* Success or failure.
- *Dependencies:* Requires configured communication channels.
- *Related Domain Events/Actions:* Action that produces *AppointmentReminderIssued* (event).

====== [.removed]#Mark No-Show#

----
markNoShow(appointmentBook: AppointmentBook, appointmentId: AppointmentId): Result Unit Error
----

- *Description:* Marks an appointment as unattended.
- *Conceptual Inputs:* Appointment book, appointment ID.
- *Conceptual Output:* Success or failure.
- *Dependencies:* Triggered after appointment time passes.
- *Related Domain Events/Actions:* Action that produces *NoShowRecorded* (event).

====== [.removed]#Record Referral#

----
recordReferral(referralRegistry: ReferralRegistry, patient: Patient, provider: Provider, referral: Referral): Result Unit Error
----

- *Description:* Registers a referral required for consultation eligibility.
- *Conceptual Inputs:* Referral registry, patient, provider, referral.
- *Conceptual Output:* Success or failure.
- *Dependencies:* Requires external provider input.
- *Related Domain Events/Actions:* Action that produces *ReferralRecorded* (event).

====== [.removed]#Record Insurance Approval#

----
recordInsuranceApproval(insuranceRegistry: InsuranceRegistry, patient: Patient, provider: Provider, appointmentId: AppointmentId): Result Unit Error
----

- *Description:* Stores insurance approval for a scheduled appointment.
- *Conceptual Inputs:* Insurance registry, patient, provider, appointment ID.
- *Conceptual Output:* Success or failure.
- *Dependencies:* Requires insurer validation.
- *Related Domain Events/Actions:* Action that produces a coverage authorization event and supports appointment confirmation.

====== [.removed]#Search Providers#

----
searchProviders(system: ClinicSystem, specialty: Specialty): List Provider
----

- *Description:* Retrieves providers matching a given specialty.
- *Conceptual Inputs:* Clinic system, specialty.
- *Conceptual Output:* List of providers.
- *Dependencies:* None (public operation).
- *Related Domain Events/Actions:* Supports provider discovery behavior.

====== Generate Available Slots

----
generate_available_slots(availability: DailyAvailability): List[datetime]
----

- *Description:* [.changed]#Generates all concrete start datetimes for valid slots on a given day from a pre-built availability definition, without touching the database. The caller is responsible for fetching availability rules and filtering out slots that conflict with existing appointments.#
- *Conceptual Inputs:* [.changed]#A DailyAvailability instance containing the date, working hours, blocked periods, and slot duration in minutes.#
- *Conceptual Output:* [.changed]#A sorted list of datetimes representing available slot start times, or a ValueError if slot duration is not positive.#
- *Dependencies:* [.changed]#Pure function; depends on the caller supplying a valid DailyAvailability with working hours, blocked periods, and slot interval already resolved.#
- *Related Domain Events/Actions:* Supports *TimeSlotPublished*.

====== Check Access

----
checkAccess(session: SupabaseSession, userRoleCache: SessionStorage): Promise<void>
----

- *Description:* [.changed]#Verifies that the current user holds the admin role before granting access to the dashboard. Checks the session cache first; if absent, queries the user_roles table and validates the role. Redirects to login if no session exists, or to home if the role is not admin. On success, caches the role in session storage and loads the user profile.#
- *Conceptual Inputs:* [.changed]#Active Supabase session and the cached role value from session storage.#
- *Conceptual Output:* [.changed]#Authorized UI state with loaded profile, or a redirect to login or home depending on the failure reason.#
- *Dependencies:* [.changed]#Requires an active Supabase auth session, a user_roles table with a joined roles name, and session storage for role caching.#
- *Related Domain Events/Actions:* [.changed]#Governs all domain actions.#

====== Book Admin Appointment

----
book_admin_appointment(*, patient_id: str, doctor_id: str, appointment_date: date, appointment_time: time, status: Literal["pending", "confirmed"] = "confirmed", notes: str | None = None, accept_waitlist: bool = False, allow_override: bool = False): tuple[dict | None, tuple[dict, int] | None]
----

- *Description:* [.changed]#Represents the full interaction flow of an admin booking an appointment for a patient, orchestrating slot validation, capacity checks, and fallback strategies through MAIN, WAITLIST, OVERRIDE, and REJECT paths.#
- *Conceptual Inputs:* [.changed]#Patient ID, doctor ID, appointment date and time, optional status (defaults to confirmed), optional notes, a flag to accept waitlist placement, and a flag to allow override booking.#
- *Conceptual Output:* [.changed]#A result dictionary with the appointment record, booking type, assigned and requested datetimes, and a message; or an error tuple with details and HTTP status code.#
- *Dependencies:* [.changed]#Composes multiple domain functions: _validate_requested_slot, _get_doctor_clinic_id, _get_main_capacity, _count_appointments_by_type, _count_waitlist_for_day, _find_next_available_slot_same_day, and _insert_appointment.#
- *Related Domain Events/Actions:* [.changed]#Maps to booking-related behavior scenarios.#
